#pragma once

#include <string>

static std::string replace_all(const std::string &str, 
                               const std::string &to, 
                               const std::string &from);